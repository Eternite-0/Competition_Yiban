# 旧花名册工具

这些文件是 Flyway 之前一次性生成、检查和导入 2024 花名册时留下的工具与样例文件。
它们保留旧机器绝对路径、旧数据库端口或旧密码，仅用于追溯当时的数据处理过程。

当前项目以以下 Flyway 迁移为准：

- `V3__seed_students.sql`：院系、班级、花名册和账号
- `V4__seed_scores.sql`：综测成绩

不要直接运行本目录脚本，也不要用它们覆盖当前 baseline。需要重新导入数据时，应新增
正式 Flyway 迁移或通过应用提供的管理接口完成。
